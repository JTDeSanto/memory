// import Card from './components/Card';
// import App from './App'

// const app = {App}
// const card = {Card}

const helpers = {
  sayMyName: function(myName, state) {
    
    // console.log(card)
    if (state === false){          
        console.log(myName)
        console.log(state)
      }
  }
}

export default helpers;